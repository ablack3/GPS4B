# Public hostname for the routing instance. Point an A record at the VM first;
# Caddy provisions the TLS certificate on startup and fails without a
# resolvable name.
DOMAIN=routing.gps4b.org

# Contact address Let's Encrypt uses for expiry warnings.
ACME_EMAIL=ops@gps4b.org
